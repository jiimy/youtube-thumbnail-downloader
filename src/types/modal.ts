export type ChildrenModalType = {
  setOnModal: React.Dispatch<React.SetStateAction<boolean>>,
  children: React.ReactNode,
  dimClick?: boolean,
  isDim?: boolean,
  onClose?: boolean,
  className?: string
}

export type ExportModalType = {
  setOnModal: React.Dispatch<React.SetStateAction<boolean>>,
  dimClick?: boolean,
  isDim?: boolean,
  onClose?: boolean,
  className?: string
}

